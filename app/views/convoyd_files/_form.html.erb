<%= form_for([@user, @convoyd_file], :html => {:multipart => true}) do |f| %>
 	<% if @convoyd_file.errors.any? %>
   		<div id="error_explanation">
     		<h2><%= pluralize(@convoyd_file.errors.count, "error") %> prohibited this file from being uploaded:</h2>
     		<ul>
     			<% @convoyd_file.errors.full_messages.each do |msg| %>
       				<li><%= msg %></li>
     			<% end %>
     		</ul>
   		</div>
 	<% end %>

 	<%= f.hidden_field :user_id, :value => @user.id %>
	<p>Select your file</p>
 	<div class="field">
   		<%= f.file_field :upload %>
 	</div>
	<p>Tell us who we should email.  Separate multiple emails with a comma.</p>
	<div class="field">
   		<%= f.text_field :notification %>
 	</div>
	<p>Give file a random password?</p>
	<div class="field">
   		<%= f.check_box :set_pass %>
 	</div>
 	<div class="actions">
   		<%= f.submit "Upload!" %>
 	</div>
<% end %>
